{
    "tasks": [
        {
            "type": "shell",
            "label": "build",
            "command": "cmake",
            "args": [
                "--build",
                "build"
            ],
            "group": "build"
        }
    ]
}
